/**
 * Normaliza un string a minúsculas.
 * @param str - El string a normalizar.
 * @returns El string normalizado en minúsculas.
 */
const normalizeStringToLowerCase = (str: string): string => {
  return str.toLowerCase();
};

export default normalizeStringToLowerCase;
